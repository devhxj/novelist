# 5月28日 前端开发记录

## 已验证

- Wails v2 + CGO + ONNX Runtime + sqlite-vec 编译运行正常，`make dev` 全链路通过
- `http://localhost:34115` 可在 Windows 浏览器调试，看到真实渲染效果

## 当前状态

基于 `3fec681`，已提交的内容：
- InitView（初始化页，平台感知默认路径）
- NovelListView（小说卡片网格 + 创建）
- BookCover / NovelCard 组件
- useApp hook（Wails IPC 封装）
- App.tsx 视图状态机（loading → init → novel-list）

## 下一步

以 `docs/frontend/design.md` 为准，逐个 view 实现，一个 view 一个 commit。

优先级：
1. NovelHubView — 小说功能菜单（2×2 大卡片），点击小说后显示
2. EditorView — 三栏编辑器（章节列表 | Monaco | 聊天区）
3. 翻书动画 — NovelListView → NovelHubView 过渡

## 注意事项

- 每个文件不超过 150 行，拆组件要果断
- Wails dev 后用 `http://localhost:34115` 在 Windows 浏览器看效果，不要在 WSL GTK 窗口判断 UI
- 用 shadcn/ui 组件库 + Tailwind，不要换框架
- 一个 view 做完 review 没问题再 commit，然后做下一个
- 不要混用 skill 和手写——一个 view 只用一个方式生成
- GitHub 链接（`github.com/sigpanic/goink`）放在 App 顶层，不要放进 NovelListView 内部



## 关键路径

```
Wails dev:    make dev  (或 wails dev -tags webkit2_41)
浏览器开发:   http://localhost:34115
前端构建:     npm --prefix frontend run build
Go 模块:      novel
类型别名:     @/ → frontend/src/
数据目录:     ~/.goink/ (Unix) | D:\Goink\ (Windows)
配置指针:     ~/.goink/config.json
wails 绑定:   frontend/src/lib/wailsjs/ (自动生成，须 commit)
GitHub:       https://github.com/sigpanic/goink
```
